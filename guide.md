You are an expert frontend developer and technical writer. Create a COMPLETE, PROFESSIONAL CSS Reference Guide as a single well-structured HTML file with the following requirements:

## SCOPE — Cover Every CSS Topic:
- CSS Basics (syntax, selectors, specificity, cascade, inheritance)
- Box Model (margin, padding, border, outline, box-sizing)
- Typography (font-family, font-size, line-height, letter-spacing, text-align, text-decoration, @font-face, Google Fonts)
- Colors & Backgrounds (hex, rgb, hsl, gradients, background-image, background-position, blend modes)
- Display & Positioning (block, inline, inline-block, none, position: static/relative/absolute/fixed/sticky, z-index, overflow)
- Flexbox (all properties for container & items, with alignment cheatsheet)
- CSS Grid (grid-template, areas, auto-fit, minmax, gap, named lines)
- Responsive Design (media queries, mobile-first approach, breakpoints, clamp(), fluid typography)
- Pseudo-classes & Pseudo-elements (:hover, :nth-child, :focus, :not, ::before, ::after, ::placeholder, etc.)
- CSS Variables (custom properties, :root, dynamic theming)
- Transitions & Animations (@keyframes, animation shorthand, transition, timing functions, will-change)
- Transforms (translate, rotate, scale, skew, matrix, 3D transforms, perspective)
- CSS Filters & Effects (filter, backdrop-filter, box-shadow, text-shadow, opacity, mix-blend-mode)
- CSS Functions (calc(), min(), max(), clamp(), var(), env(), attr())
- Selectors Deep Dive (combinators, attribute selectors, :is(), :where(), :has())
- CSS Logical Properties (margin-inline, padding-block, inset)
- CSS Shapes & Clip-path
- Scrollbar Styling
- Print Styles (@media print)
- CSS Architecture & Best Practices (BEM, naming conventions, resets, normalizing)
- Modern CSS (container queries, :has(), cascade layers @layer, subgrid, accent-color, color-mix())

## FORMAT REQUIREMENTS:
- Single HTML file with embedded CSS and JS
- Dark/light mode toggle button
- Sticky sidebar navigation with all section links
- Each section must have:
  * Clear explanation in plain English
  * Syntax block (styled code snippet)
  * LIVE visual example rendered directly in the page (not just code — actually show it visually)
  * Common mistakes / gotchas callout box
  * Browser support note where relevant
- Color-coded code blocks with syntax highlighting
- Copy-to-clipboard button on every code block
- Search/filter bar to find topics quickly
- Progress indicator showing how far down the page the user has scrolled
- Beautiful, professional design — clean typography, consistent spacing, subtle shadows
- Fully responsive (mobile, tablet, desktop)
- Table of contents at the top with anchor links

## QUALITY BAR:
- This is meant to be the ONLY CSS reference a developer ever needs
- Every example must be self-contained and actually render visually
- Explanations should be beginner-friendly but detailed enough for intermediate developers
- Include real-world use cases, not just toy examples
- Add a "Pro Tip" callout for non-obvious tricks in each major section

Generate the complete, untruncated HTML file now. Do not stop until the entire guide is finished.